<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sports_match_day">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".di.SportsApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_sports_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_sports_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsMatchDay"
        tools:replace="android:allowBackup">

        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/Theme.SportsMatchDay.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SportsMatchDay.NoActionBar"
            android:windowSoftInputMode="adjustPan"/>

        <service android:name=".firebase.MessageService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>